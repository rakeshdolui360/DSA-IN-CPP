# DSA IN CPP
A structured DSA in C++ repository featuring implementations of essential data structures (arrays, linked lists, stacks, queues, trees, heaps, graphs) and popular algorithms (sorting, searching, recursion, DP, backtracking, greedy). Includes clean code, comments, and multiple approaches to key problems.
